export default class ScoreResult {
  constructor(topic, answers, score, fundingPriorities) {
    this.topic = topic
    this.answers = answers
    this.score = score
    this.fundingPriorities = fundingPriorities
  }
}
